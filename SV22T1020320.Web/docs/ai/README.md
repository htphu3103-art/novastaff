# docs/ai — FE shards (token-optimized)

**Agent:** [`../../AI_PROJECT_CONTEXT.md`](../../AI_PROJECT_CONTEXT.md) → 1 shard · ≤2 `src/` files.

**Maintainer:** sau refactor lớn → Semble `search` repo `SV22T1020320.Web` cập nhật shard tương ứng.
